import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { Top10Page } from './top10';

@NgModule({
  declarations: [
    Top10Page,
  ],
  imports: [
    IonicPageModule.forChild(Top10Page),
  ],
})
export class Top10PageModule {}
